import { StyleSheet, Text, View, SafeAreaView } from 'react-native'
import React from 'react'
import { AllDone } from '../../../Components'

const Success = () => {
  return (
    <SafeAreaView>
      <AllDone/>
    </SafeAreaView>
  )
}

export default Success

const styles = StyleSheet.create({})